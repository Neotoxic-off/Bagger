<UserControl x:Class="Bagger.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Bagger.Views"
             xmlns:viewmodels="clr-namespace:Bagger.ViewModels"
             mc:Ignorable="d"
             Background="Transparent"
             d:DesignHeight="700" d:DesignWidth="1200">

    <UserControl.DataContext>
        <viewmodels:MainViewModel/>
    </UserControl.DataContext>

    <Grid>
        <StackPanel>
            <ListBox ItemsSource="{Binding Proxy.Requests}" Height="200" Foreground="White"/>
            <Label Content="{Binding Proxy.Token}" Foreground="White"/>
            <Button Content="{Binding Proxy.Running}" Command="{Binding ChangeStateProxyCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
